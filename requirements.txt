streamlit>=1.60,<1.61
# Streamlit 1.60's custom GZip middleware still uses Starlette's 0.x
# responder signature. Starlette 1.x adds a required thread_minimum_size
# argument and crashes before the Streamlit script can run.
starlette==0.49.3
pandas>=2.2,<3.0
plotly>=6.0,<7.0
